---
allowed-tools: [Bash, AskUserQuestion]
---

# p. workflow $ARGUMENTS

```bash
prjct workflow $ARGUMENTS --md
```

Follow the CLI output.

If adding a rule and the syntax is unclear, ASK:
- What command? (task, done, ship, sync)
- Before or after?
- Is it a gate (must pass to continue) or a hook (run but don't block)?
